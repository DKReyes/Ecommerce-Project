<h1 class="title is-2">All Categories</h1>

<div>
  <ul>
    <% @categories.each do |category| %>
      <li>
        <%= link_to category.name, category %>
      </li>
    <% end %>

    <%= paginate @categories %>
  </ul>
</div>